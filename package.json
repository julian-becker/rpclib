{
    "watch": {
        "test": {
            "patterns": [
                "rpc/**",
                "rpc_test/**"
            ],
            "extensions": "h,cpp,txt"
        }
    },
    "scripts": {
        "test": "cmake --build build",
        "watch": "npm-watch"
    },
    "dependencies": {},
    "devDependencies": {
        "npm-watch": "^0.6.0"
    }
}
